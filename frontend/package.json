{
  "name": "blender-agent-ui",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.4.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "start": "serve -s build -l 3000",
    "build": "react-scripts build",
    "dev": "REACT_APP_API_URL=http://localhost:8000 react-scripts start"
  },
  "devDependencies": {
    "ajv": "^6.12.6"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
